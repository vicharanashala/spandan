# Allowed Email Domains Configuration
# Add allowed email domains, wildcards, or regex patterns below (one per line).
# If this file is empty or only contains comments, all email domains are allowed by default.
# Lines starting with '#' or '//' are treated as comments.
#
# Examples (uncomment to enable):
# example.com
# *.edu.in
# /^[a-z0-9-]+\.org$/i
