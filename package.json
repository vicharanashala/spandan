{
  "name": "spandan",
  "version": "0.1.0",
  "private": true,
  "description": "Spandan - Poll Question Generator",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently -n FRONTEND,BACKEND,WHISPER -c blue,green,magenta \"npm run dev:frontend\" \"npm run dev:backend\" \"npm run dev:transcription\"",
    "dev:frontend": "npm run dev --workspace=frontend",
    "dev:backend": "npm run dev --workspace=backend",
    "dev:transcription": "python backend/transcription_server.py",
    "install:all": "npm install && npm install --workspace=frontend && npm install --workspace=backend && pip install -r backend/requirements.txt"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "pm2": "^7.0.1"
  }
}